// Sample JavaScript file

// Declaring variables
let name = "John";
let age = 30;

// Function to greet a person
function greetPerson(name, age) {
    console.log(`Hello, ${name}! You are ${age} years old.`);
}

// Calling the function to greet the person
greetPerson(name, age);
